# Core dependencies for the project

# Module for working with dates, including ISO 8601 formats and time zone support
python-dateutil>=2.8.2

# Library for generating QR codes (comment for qrcode, if the commented-out dependency is planned to be used)
qrcode[pil]>=7.3.1

# Cryptographic library for working with certificates, keys, and encryption algorithms
cryptography>=41.0.0

# Framework for testing Python applications, supporting unit, integration, and functional tests
pytest>=7.4.0
pytest-mock>=3.10.0
pandas>=2.0.0

# Library for HTTP requests, convenient for API interaction, sending GET/POST requests, and handling responses
requests>=2.31.0

# Library for generating QR codes, supporting data of various formats
pyqrcode==1.2.1

# Library for creating web interfaces in Python with minimal effort (used for the admin panel)
gradio>=3.36.0

# Library for handling PNG images (dependency for the pyqrcode library)
pypng

# Library for working with processes and system information, useful for monitoring and managing resources
psutil

# For adding colors and formatting text in console outputs
termcolor

# Library for creating text-based tables, used in reports
prettytable>=3.7.0

# Library for data visualization
matplotlib

# Memory profiling library
memory-profiler

# Library for object graph visualization
objgraph

# HTTP request library
requests>=2.31.0

# Library for JSON logging
python-json-logger>=2.0.7
